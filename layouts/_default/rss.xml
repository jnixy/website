<rss version="2.0">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Site.BaseURL }}</link>
    <description>{{ .Site.Params.description }}</description>
    <language>{{ .Site.LanguageCode }}</language>

    {{ range .Pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Summary | html }}</description>

      {{ with .Params.featured_image }}
      {{ $image := .Page.Resources.GetMatch . }}
      {{ if $image }}
      <enclosure url="{{ $image.Permalink }}" type="{{ if strings.HasSuffix $image.Name ".png" }}image/png{{ else if strings.HasSuffix $image.Name ".jpg" }}image/jpeg{{ else if strings.HasSuffix $image.Name ".jpeg" }}image/jpeg{{ else }}application/octet-stream{{ end }}"/>
      {{ else }}
      <!-- Image not found -->
      {{ end }}
      {{ end }}
    </item>
    {{ end }}
  </channel>
</rss>
